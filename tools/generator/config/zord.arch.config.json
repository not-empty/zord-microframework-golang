{
    "replacers": {
        "dinamics": [
            "{{domain}}",
            "{{domainCap}}"
        ],
        "statics": {
            "//codeGen1": "{{domain}}ListRoutes := New{{domainCap}}Routes(logger, Environment, MySql, idCreator)\n    //{{codeGen1}}"
        }
    },
    "stubs": {
        "domain": {
            "toPath": "application/domain/{{domain}}/",
            "fromPath": "tools/generator/stubs/domain/"
        },
        "services": {
            "toPath": "application/services/{{domain}}/",
            "fromPath": "tools/generator/stubs/services/"
        },
        "repositories": {
            "toPath": "application/domain/{{domain}}/",
            "fromPath": "tools/generator/stubs/domain/"
        }
    }
}
