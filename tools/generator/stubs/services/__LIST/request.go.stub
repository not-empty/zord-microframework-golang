package {{domain}}

import (
	domain "go-skeleton/application/domain/{{domain}}"
	{{validatorImport}}
)

type Request struct {
	{{domainCap}} []domain.{{domainCap}}
	Err   error
	{{validator}}
}

func NewRequest({{validator}}) Request {
	return Request{
		{{validatorInject}}
	}
}

func (r *Request) Validate() error {
	{{validatorRule}}
	return nil
}
