package {{domain}}

type {{domainCap}} struct {
	{{domainCap}}Id   string `gorm:"type:char(26);primarykey"`
}

type Repository interface {
	Get(id string) (*{{domainCap}}, error)
	Create(*{{domainCap}}) error
	List() (*[]{{domainCap}}, error)
	Edit(*{{domainCap}}) error
	Delete(*{{domainCap}}) error
}
